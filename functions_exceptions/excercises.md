# Functions and exceptions practice

```
1. Write function according to following signature print_range(begin: int, end:int) and prints range of numbers from begin to end to the screen.

2. Write function according to following signature get_range_numbers(begin: int, end: end, even: bool = True) -> list. 
If even is True function return list of even numbers, elese odd numbers.

3. Write funtion _random_list(begin: int, end: int, amount: int, exclude: list)_ that creates and returns list of given amount with randomly selected elements excluding those that were given in exclude list. 
```
